import React from "react";
import { Button } from "./ui/button";

const ShowMore = () => {
  return (
    <div className="w-full flex justify-center items-center">
      <Button
        onClick={() => {}}
        className="bg-blue-700 text-white rounded-full text-center text-base font-medium"
      >
        Show More
      </Button>
    </div>
  );
};

export default ShowMore;
